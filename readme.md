This is:

```
JSON DSL → 
    YomiKaze (parser) → 
    AST →
    KamiKaze (code generator) →
    C++ codegen →
    cmake →
    libkaze.so →
    TheTailor (native loader) →
    FFI →
    fs2 stream
```
